server:
  port: 8080
  servlet:
    context-path: /web

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: "*"

app:
  locale:
    default: ja # ja, vi, en,...

  slf4j-filter:
    request-header: # Can leave empty on production, set it on development and staging if you want to debug
    response-header: 'X-Client-Id'

  logging:
    enable-log-request: true
    enable-log-response: false
    log-response-time:
      enable: false
      exclude: >
      include: >
    debug: false
